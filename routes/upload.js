import express from 'express';

const router = express.Router();

// Audio upload functionality temporarily disabled
// This endpoint will be re-enabled in a future version
// For now, focus on song documentation (chords, lyrics, notes)

export default router;
